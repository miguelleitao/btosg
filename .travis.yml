
language: cpp
sudo: false # only for faster builds
install:
  - sudo apt update && sudo apt -y install openscenegraph libopenscenegraph-dev
script:
  - make all

after_success:
  - bash <(curl -s https://codecov.io/bash)
